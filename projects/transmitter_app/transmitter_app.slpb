---
parameters:
- name: "build_dir"
constants:
- name: "project_name"
  value: "transmitter_app"
steps:
- task: "create_gbl"
  output: "{{build_dir}}/{{project_name}}.gbl"
  app: "{{build_dir}}/{{project_name}}.out"
